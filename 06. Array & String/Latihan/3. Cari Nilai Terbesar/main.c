#include <stdio.h>

int main() {
    int arr[] = { 10, 30, 20, 50, 40 };
    int max = 0;

    for ( int i = 0; i < arr[i]; i++ ) {
        if ( max < arr[i] ) {
            max = arr[i];
        }
    }

    printf("%d", max);

    return 0;
}
