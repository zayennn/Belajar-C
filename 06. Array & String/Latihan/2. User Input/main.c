#include <stdio.h>

int main() {
    int n[5];

    for ( int i = 0; i < 5; i++ ) {
        printf("nilai ke - %d : ", i + 1);
        scanf("%d", &n);
    }

    for ( int i = 0; i < n[i]; i++ ) {
        printf("%d\n", n[i]);
    }

    return 0;
}